Global setup:

 Download and install Git
  git config --global user.name "sam"
  git config --global user.email info@isimpledesign.co.uk
        

Next steps:

  mkdir ams3mobile
  cd ams3mobile
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:isimpledesign/ams3mobile.git
  git push -u origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:isimpledesign/ams3mobile.git
  git push -u origin master
      

Importing a Subversion Repo?

  Click here
      

When you're done:

  Continue

